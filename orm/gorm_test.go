package orm

import (
	"testing"
)

func TestNewDB(t *testing.T) {
	//NewDB("a")
	//NewDB("b")
	//NewDB("c")

	// only example
	//Gorm().GetORM().Select("id").Where("id = ?", 1).First(nil)

	//Gorm().Close()
}
