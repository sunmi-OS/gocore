package orm

import (
	"testing"
)

func TestNewDB(t *testing.T) {
	//NewGorm("a")
	//NewGorm("b")
	//NewGorm("c")

	// only example
	//Gorm().GetORM().Select("id").Where("id = ?", 1).First(nil)

	//Gorm().Close()
}
